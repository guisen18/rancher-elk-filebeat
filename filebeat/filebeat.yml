output:
  logstash:
    enabled: true
    hosts:
      - logstash:5000
    timeout: 60

filebeat:
  inputs:
#    -
#      paths:
#        - /var/log/syslog
#      document_type: syslog
    - type: container
      paths:
        - '/usr/share/dockerlogs/data/*/*.log'
